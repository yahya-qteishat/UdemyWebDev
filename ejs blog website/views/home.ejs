<%-include('partials/header')%>


<h1>Home</h1>

<p><%=homeStartingContentKey%></p>

<!-- for(var i = 0 ; i < posts.length ; i++) -->
<% posts.forEach(function(individualPost){ %>
  <h1> <%=individualPost.title%> </h1>
  <p>
    <%=individualPost.content.substring(0,100) + "..."%>
    <a href="/posts/<%=individualPost.title%>">read more</a>
  </p>
  <%# cutting short blog post to 100 letters on the home page %>

<% }) %>

<%# here we use the EJS value tag to embed the actual value of those variables into the h1 and p tags %>

<%-include('partials/footer')%>
